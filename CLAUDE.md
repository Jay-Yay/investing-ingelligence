# 프로젝트 지침

## 지침 자동 개선

작업 중 실수를 했거나(사용자가 정정) 시행착오 끝에 더 나은 방법을 찾은 경우, 사용자가
별도로 요청하지 않아도 그 교훈을 곧바로 이 파일에 반영한다:

- **사용자가 결과를 정정한 경우**: 무엇이 잘못됐는지, 왜 그런 실수가 났는지 원인을
  먼저 파악한 뒤, 같은 실수를 막을 구체적 규칙을 추가한다. 원인 파악 없이 표면적인
  규칙만 추가하지 않는다.
- **시행착오 끝에 더 나은 방법을 찾았거나, 사용자가 특정 접근을 명시적으로
  승인/확인한 경우**: 그 방법을 향후 기본값으로 기록한다.
- 반영 후에는 "OO 지침을 CLAUDE.md에 반영했습니다"라고 짧게 알리되, 되묻거나
  승인을 구하지 않고 바로 반영한다. 단, 투자 원칙(`Investment_Mandate.md`)이나
  포트폴리오 가드레일 등 핵심 방향성과 충돌할 소지가 있으면 먼저 확인을 구한다.
- 관련성이 있으면 auto-memory에도 feedback 타입으로 함께 기록해 배경(왜 이 규칙이
  생겼는지)을 남긴다 — CLAUDE.md는 규칙 자체, 메모리는 그 규칙이 생긴 사연을
  담당한다.
- 규칙이 누적되며 중복되거나 서로 모순되는 항목이 생기면, 새로 추가하기보다
  기존 항목을 통합/수정한다(파일이 무한정 불어나지 않도록).

## 리서치/분석 답변의 출처 표기

시장 상황, 종목, 원인 분석 등 리서치성 답변(브리핑 채팅 답변, vault에 저장하는
브리핑 md 파일 모두 포함)을 작성할 때는 근거로 쓴 출처를 **각 요약 문장/불릿 안에
인라인 마크다운 하이퍼링크**로 붙인다. 예:

> **[숏셀러 공격 — Hunterbrook Capital](https://example.com/article)** — 공급망
> 허위진술 의혹 제기...

인라인으로 붙이기 애매한 경우(여러 출처가 섞였거나 위치상 어색한 경우)는 답변 맨
뒤에 "전체 소스" 목록으로 링크를 모아도 된다. 링크 없이 출처만 말로 언급하고
넘어가지 않는다. WebSearch 결과, vault 내부 문서(Naver/Telegram/DART 등) 모두
동일하게 적용 — 내부 vault 문서는 `source_url`/`원문` 링크를 사용한다.

## 포트폴리오 종목 분석 형식

사용자가 "포트폴리오 종목들 최근 스크랩/수집 정보 기반으로 분석해줘"라고 요청하면
(analyze 실행 직후든, 이미 분석된 자료를 바탕으로 하든), `vault/30_Portfolio/portfolio.yaml`의
보유 종목 전체를 대상으로 종목별로 아래 3단 구조를 사용한다:

```
## <종목명>(<티커>) — <Investment_Mandate.md 종목군별 렌즈 한 줄>

**핵심 발견**: 최근 수집된 원문(뉴스/공시/블로그/IB리포트 등)에서 확인된 사실을
근거로. 위 "리서치/분석 답변의 출처 표기" 규칙대로 인라인 하이퍼링크 필수.

**주가 영향 경로**: 그 발견이 왜/어떻게 주가에 영향을 주는지 — bull 해석과 bear
해석을 모두 짚고, 지금 상황이 이미 주가에 반영된 것인지 아닌지 판단.

**지켜볼 포인트**: 향후 방향을 실제로 검증해줄 구체적 이벤트/지표 — 가능하면
날짜(다음 실적 발표일 등)와 어떤 숫자·문구가 나오면 강세/약세로 판정할지까지 명시.
```

- 각 종목의 렌즈는 `Investment_Mandate.md`의 "종목군별 렌즈" 표를 따른다(예: 메모리는
  사이클 타이밍, 소비재는 브랜드 확장, NBIS는 AI 인프라 수요/공급, BE는 전력
  인프라, RDDT는 플랫폼 수익화).
- "핵심 발견"은 실제로 읽은 원문에 근거해야 하며 지어내지 않는다 — 근거가 부족하면
  해당 종목은 "오늘 새로 확인된 내용 없음"으로 짧게 처리.
- 채팅 답변이 너무 압축되어 실제 추출 깊이를 못 담을 것 같으면, vault에 이미
  기록된 각 문서의 "## 핵심 주장" 섹션을 답변에서 요약하지만 뭉개지 않는다 —
  최소한 각 종목당 2~4개의 구체적 근거는 담는다.

## 정량 수치 인용 시 주의사항

- %, 가격, 재무수치 등 시간에 따라 변하는 정량 지표를 스크랩 원문에서 인용할 때는
  그 원문의 발행일(`published_at`)을 반드시 확인한다. 발행일이 오늘이 아니면
  "오늘 확인된" 것처럼 쓰지 말고 "OO월 기준"이라고 시점을 명시한다. (예: 5월 기사의
  "현재 32%"를 7월 분석에 그대로 쓰면 안 됨 — 실제 3개월 전 스냅샷.)
- 범위·성격이 다른 두 수치를 한 문장에 섞어 쓰지 않는다. 예를 들어 특정 투자자
  1인의 신규 대량보유 공시(개별 지분율)와 회사 전체 외국인 지분율 추이는 서로
  다른 지표다 — 나란히 쓰면 서로를 확증하는 것처럼 오독될 수 있으니 반드시
  구분해서 서술한다.
- 외국인 지분율·현재가·컨센서스처럼 자주 바뀌는 수치가 분석의 핵심 근거로
  쓰일 때는, vault 스크랩 데이터만으로 확정하지 말고 가능하면 WebFetch/WebSearch로
  최신값을 한 번 더 대조한다. 대조가 어려우면 스크랩 시점을 명시하고 "최신 수치와
  다를 수 있음"을 밝힌다.

## 공시(DART) 해석 원칙

- **대량보유상황보고서(5% 공시)의 지분율 변동을 대표보고자 개인의 매매로 읽지
  않는다.** 이 보고서는 본인 + 특별관계자·공동보유자 전체를 대표보고자 1인 명의로
  묶어 신고한다. 최대주주 지분율이 크게 떨어져 보여도 실제 매도 주체는 공동보유
  VC·FI인 경우가 많으므로, 반드시 원문 "세부변동내역"에서 **성명(명칭)별 매도자**를
  확인한 뒤 서술한다. (2026-08-08 달바글로벌 분석에서 VC 매도를 창업자 매도로
  잘못 귀속시킨 사례.)
- **내부자 매매는 `임원ㆍ주요주주 특정증권등 소유상황보고서`로 확인한다.** OpenDART
  `elestock.json`으로 목록을, `document.xml`로 원문을 받아 세부변동내역 표를 파싱하면
  **일자별 수량·단가·거래방법(장내/장외/증여/스톡옵션 행사)**까지 나온다. 언론 기사
  요약에 의존하지 않는다.
- **매도에는 반드시 목적을 확인한다.** 일정 규모 이상의 대주주·임원 거래는
  `임원ㆍ주요주주 특정증권등 거래계획보고서`로 최소 30일 전 사전공시되며, 여기에
  매도 사유(세금 납부·상속·담보 상환 등)와 계획 수량·기간이 기재된다. 사유 확인
  없이 "내부자 매도 = 약세 신호"로 단정하지 않는다. 스톡옵션 행사가와 매도가를
  함께 보면 "차익 실현"인지 "전망 비관"인지 구분된다.
- 소유상황보고는 변동일로부터 5영업일 이내 제출이므로, **최근 1주일치는 아직
  공시 전일 수 있다** — "현재까지 확정된 건 O월 O일까지"라고 명시한다.

## 13F 해석 원칙

- **행 수를 종목 수로 읽지 않는다.** 13F는 같은 종목을 운용 재량 구분·공동 운용사·put/call별로
  여러 행으로 나눠 보고한다(실측: 121개 행 = 37개 종목). 보유 종목 수·비중·최대 비중 종목은
  반드시 `(CUSIP, put_call)` 기준으로 합산된 값을 써야 한다. vault 문서 머리글의
  "보유 포지션 수 N (원문 K행)"에서 N을 쓰고, 표의 `원문행수`가 1보다 크면 그 행은 합산 결과다.
- **금액 단위는 제출일로 판단한다.** SEC는 2023-01-03 이후 제출본의 금액을 **원 달러**로,
  그 전은 **천 달러**로 받는다. 연도가 섞인 시계열·랭킹 비교를 할 때 이 구분을 놓치면
  1,000배 차이가 조용히 섞인다.
- **`legacy_units` 경고가 붙은 스냅샷의 금액·비중은 인용하지 않는다.** 2026-08-25 이전
  수집분(198건 전량)이 여기 해당하며, 재수집 전까지 값을 보정할 방법이 없다. 개수·기간 등
  단위와 무관한 항목만 쓰고, 금액이 필요하면 원문 링크를 제시한다.

## 원문 품질 확인

- 수집된 문서를 근거로 인용하기 전에 frontmatter의 `readable_ratio`를 본다. 0.95 미만이면
  본문이 깨진 문서(주로 옛 DART 인코딩 오류 211건)이므로 **근거로 쓰지 않고** 원문 링크만
  제시한다.
- `truncated: true`면 본문이 40,000자에서 잘린 문서다. "원문에 그런 언급이 없다"는 식의
  부재 판단(negative claim)을 이 문서로 하지 않는다 — 잘린 뒷부분에 있을 수 있다.
- `content_capture.mode: metadata_only`는 본문을 아예 못 가져온 문서다(실측 1,534건).
  제목·링크만 있으므로 내용에 대한 서술을 하지 않는다.
- 종목 관계는 `entities.mentions`(분석 대상)와 `entities.analyst_house`(리포트를 쓴 증권사)를
  구분해서 읽는다. 증권사를 분석 대상으로 착각하면 "교보증권 목표주가"처럼 주체와 대상이
  뒤바뀐 서술이 나온다.

## 질문 분석 및 답변 태도

- **정보 부족 시 질문 생성 중지, 역질문**: 질문에 실전 적용 가능한 인사이트 있는
  답변을 하기에 정보가 부족하면(범위가 모호하거나, 핵심 전제·기준이 빠졌거나,
  여러 해석이 가능한 경우) 곧바로 답변을 생성하지 말고, 부족한 정보를 채울 역질문을
  먼저 한다. 질문의 핵심 의도를 확인하지 않은 채 넘겨짚어 답하지 않는다.
- **정확성 최우선, 검증된 사실 기반**: 창의성보다 정확성을 우선한다. 확실하지 않은
  내용은 추측해서 답하지 말고 "정보 부족"이라고 명시한다. 통계·날짜 등 구체적
  사실에는 가능한 한 출처를 제시하고(위 "리서치/분석 답변의 출처 표기" 규칙 적용),
  사실(Fact)과 의견(Opinion)을 구분해서 서술한다.
- **잘못된 전제는 정정**: 사용자 질문에 잘못된 전제나 사실 오류가 포함돼 있으면
  그 전제에 맞춰 답하지 말고, 반드시 오류를 먼저 지적하고 정정한 뒤 답한다.
- **어조**: 아부·빈말·불필요한 사과 금지. 감정을 배제하고 건조하고 객관적인
  어조로 필요한 정보만 직설적으로 전달한다.
- **Devil's advocate 후 종합**: 리서치/분석 답변에서는 각 소스의 주장을 반대
  관점에서 비판적으로 검토(devil's advocate)한 뒤, 찬성/반대 시각을 종합해
  결론에서 판단의 핵심 포인트·기준을 제시한다. 한쪽 시각만 전달하고 끝내지 않는다.

## Q&A 저장 위치

사용자가 채팅에서 질문을 던지고 그에 대해 리서치/분석해서 답변한 경우(정기
브리핑이 아니라 개별 질문에 대한 답변), 그 답변을 `vault/50_Reports/Brief/qna/`
폴더에 `qna_{날짜}_{시간}.md`(예: `qna_2026-07-29_0142.md`, KST 기준) 파일로
저장한다. 내용은 질문 원문 + 답변(위 "리서치/분석 답변의 출처 표기" 규칙대로
인라인 하이퍼링크 포함) + 맨 뒤 출처 목록 구조를 따른다. 정기 포트폴리오
데일리 브리핑은 `vault/50_Reports/Brief/brief_portfolio_{날짜}.md`(날짜만, 시간 없음)에
저장하고, qna 폴더와 섞지 않는다. 같은 날 재요청으로 다시 생성하면 새 시간 이름의
파일을 만들지 말고 그 날짜 파일을 덮어쓴다 — 여러 머신(Mac/Github 등)에서 같은 날
브리핑을 돌리면 내용은 사실상 같은데 시간만 다른 파일이 중복 누적되는 문제가 있었다.
